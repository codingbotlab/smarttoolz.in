# SmartToolz

Free, fast and simple online tools.

## Structure

Each public tool lives in its own folder under `tools/<tool-name>/`. Shared site code stays at the repository root.

The site is database-free, account-free, and tracking-free.
