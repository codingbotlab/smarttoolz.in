RewriteEngine On
RewriteBase /knowledge-base/

# Keep real files/directories working normally.
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Tool-specific how-to article URLs.
RewriteRule ^([^/]+)/article/?$ guide.php?tool=$1 [L,QSA]
